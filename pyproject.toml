[project]
name = "materials-tzu-chi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Wei-Cheng Chen", email = "3331363@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.6",
]

[project.scripts]
materials-tzu-chi = "materials_tzu_chi:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
